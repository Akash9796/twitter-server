import { types } from "./user.types";
import { Query } from "./user.query";
import { resolvers } from "./user.resolver";
// import { mutations } from "./user.mutation";

export const User = { types, Query, resolvers };
